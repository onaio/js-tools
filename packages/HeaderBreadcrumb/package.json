{
  "name": "@onaio/head-bread-crumb",
  "version": "0.0.1",
  "description": "Configurable Breadcrumbs Component that renders breadcrumb items with urls as links or without urls as text nodes",
  "keywords": [
    "breadcrumb "
  ],
  "author": "Ona Engineering",
  "homepage": "https://github.com/onaio/js-tools",
  "license": "Apache-2.0",
  "main": "dist/index.js",
  "types": "dist/types",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/onaio/js-tools.git"
  },
  "scripts": {
    "jest": "jest --coverage --verbose --color",
    "tsc": "tsc",
    "transpile": "babel src -d dist --root-mode upward --extensions '.ts,.tsx' --ignore '**/*.test.ts,**/*.test.tsx,**/tests,**/__tests__'"
  },
  "jest": {
    "setupFiles": [
      "../../setupTests"
    ]
  },
  "bugs": {
    "url": "https://github.com/onaio/js-tools/issues"
  },
  "dependencies": {
    "@types/reactstrap": "^8.0.1",
    "reactstrap": "^8.0.1"
  },
  "devDependencies": {
    "enzyme": "^3.10.0",
    "enzyme-to-json": "^3.3.5"
  }
}
